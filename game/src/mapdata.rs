use crate::coord::Coord;
use crate::message::{Message};


// pub fn create_map_data (coord: Coord) -> SMessage {
//
// }


struct mapdata {
    pktid: i32,
    offset: u16,
    len: u16,
}
//uint8 ver ==1
